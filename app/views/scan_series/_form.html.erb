<%= form_with(model: scan_series, local: true) do |form| %>
  <% if scan_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scan_series.errors.count, "error") %> prohibited this scan_series from being saved:</h2>

      <ul>
      <% scan_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :key %>
    <%= form.text_area :key, id: :scan_series_key %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
