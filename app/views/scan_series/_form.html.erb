<%= form_with(model: scan_series, local: true, html: { class: 'form-horizontal' }) do |form| %>
  <% if scan_series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scan_series.errors.count, "error") %> prohibited this scan_series from being saved:</h2>
      <ul>
      <% scan_series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :equipment_id, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= form.collection_select :equipment_id, current_account.equipment.all, :id, :name, { prompt: "Select equipment" }, { class: 'form-control' } %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-4 col-sm-offset-2">
      <%= form.submit class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
