<%= form_with(model: inspection_file, local: true) do |form| %>
  <% if inspection_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(inspection_file.errors.count, "error") %> prohibited this inspection_file from being saved:</h2>

      <ul>
      <% inspection_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.file_field :file %>
  </div>
<% end %>
