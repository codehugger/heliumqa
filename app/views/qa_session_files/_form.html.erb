<%= form_with(model: [@qa_session, qa_session_file], local: true) do |form| %>
  <% if qa_session_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(qa_session_file.errors.count, "error") %> prohibited this qa_session_file from being saved:</h2>

      <ul>
      <% qa_session_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.file_field :file %>
  </div>
<% end %>
