<%= form_with(model: analysis_session, local: true) do |form| %>
  <% if analysis_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(analysis_session.errors.count, "error") %> prohibited this analysis_session from being saved:</h2>

      <ul>
      <% analysis_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
