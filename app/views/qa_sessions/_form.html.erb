<%= form_with(model: qa_session, local: true, html: { class: 'form-horizontal' }) do |form| %>
<% if qa_session.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(qa_session.errors.count, "error") %> prohibited this qa_session from being saved:</h2>

  <ul>
    <% qa_session.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="form-group">
  <%= form.label :equipment_id, class: 'col-sm-2 control-label' %>
  <div class="col-sm-10">
    <%= form.collection_select :equipment_id, Equipment.all, :id, :name, { prompt: "Select equipment" }, { class: 'form-control' } %>
  </div>
</div>

<!-- <div class="form-group">
  <%= form.label :performed_at, class: 'col-sm-2 control-label' %>
  <div class="col-sm-10">
    <div class="input-group date">
      <%= form.text_field :performed_at, class: 'datepicker form-control' %>
    </div>
  </div>
</div> -->

<div class="form-group">
  <div class="col-sm-4 col-sm-offset-2">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
</div>
<% end %>
