<div class="analysis-result analysis-result-table">
  <h4><%= result["label"] || result["key"] %></h4>
  <div class="table-responsive">
    <table class="table">
      <thead>
        <% result["table_columns"].each do |column| %>
        <th><%= column["label"] || column["key"] || column %></th>
        <% end %>
      </thead>
      <tbody>
        <% result["table_rows"].each do |row| %>
        <tr>
          <% row.each do |col| %>
          <td><%= col %></td>
          <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
