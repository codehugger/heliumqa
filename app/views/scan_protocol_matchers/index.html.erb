<p id="notice"><%= notice %></p>

<h1>Scan Protocol Matchers</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scan_protocol_matchers.each do |scan_protocol_matcher| %>
      <tr>
        <td><%= link_to 'Show', scan_protocol_matcher %></td>
        <td><%= link_to 'Edit', edit_scan_protocol_matcher_path(scan_protocol_matcher) %></td>
        <td><%= link_to 'Destroy', scan_protocol_matcher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Scan Protocol Matcher', new_scan_protocol_scan_protocol_matcher_path(@scan_protocol) %>
